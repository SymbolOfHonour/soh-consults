import Link from "next/link";
import {notFound,redirect} from "next/navigation";
import {isAdmin} from "../../../lib/admin-auth";
import {isModuleKey,listModuleRecords} from "../../../lib/admin-module-store";
import PublishedUpdateModuleFeed from "../PublishedUpdateModuleFeed";
import ModuleManagerClient from "./ModuleManagerClient";
const modules:Record<string,{title:string;description:string}>= {forms:{title:"Forms Manager",description:"Add, edit and control admission forms and programme availability."},calculator:{title:"LASU Calculator Control",description:"Manage notices and configuration records without changing the protected live calculation engine."},media:{title:"Media Library",description:"Manage media records, URLs, flyers and PDF references used across S.O.H CONSULTS."},calendar:{title:"Content Calendar",description:"Manage publications, deadlines, examinations and registration dates."},seo:{title:"SEO Control",description:"Track and manage titles, metadata, links and discoverability tasks."},enquiries:{title:"Enquiries & Leads",description:"Manage website enquiries and WhatsApp lead records."},analytics:{title:"Analytics",description:"Store and manage analytics notes, reports and tracking records while deeper automated reporting is built."},settings:{title:"System Settings",description:"Manage business and website configuration records from the private dashboard."}};
export default async function Page({params}:{params:Promise<{module:string}>}){if(!(await isAdmin()))redirect("/admin/updates");const {module}=await params;if(!modules[module]||!isModuleKey(module))notFound();const item=modules[module];const records=await listModuleRecords(module);return <main className="min-h-screen bg-gray-50 text-gray-950"><header className="border-b bg-white"><div className="mx-auto flex max-w-[1300px] flex-wrap items-center justify-between gap-4 px-5 py-5"><div><p className="text-[11px] font-black uppercase tracking-[.2em] text-green-700">S.O.H CONSULTS · PRIVATE</p><h1 className="text-2xl font-black">{item.title}</h1><p className="mt-1 text-sm text-gray-500">{item.description}</p></div><div className="flex gap-2"><Link href="/admin/modules" className="rounded-xl border px-4 py-2 text-sm font-black">All Modules</Link><Link href="/admin" className="rounded-xl bg-green-700 px-4 py-2 text-sm font-black text-white">Dashboard</Link></div></div></header><section className="mx-auto max-w-[1300px] px-5 py-7">{(module==="forms"||module==="media"||module==="calendar")&&<PublishedUpdateModuleFeed module={module}/>}<ModuleManagerClient module={module} initial={records}/></section></main>}
