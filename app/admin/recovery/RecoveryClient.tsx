"use client";
import { useEffect,useState } from "react";
import type { QueuedStory } from "../../../lib/news-queue";
import type { AuditEntry,HistoryEntry } from "../../../lib/admin-recovery";
type Data={trash:QueuedStory[];history:HistoryEntry[];audit:AuditEntry[]};
export default function RecoveryClient(){const[data,setData]=useState<Data>({trash:[],history:[],audit:[]});const[tab,setTab]=useState<"trash"|"history"|"audit">("trash");const[busy,setBusy]=useState("");const[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/admin/recovery",{cache:"no-store"});const j=await r.json();if(r.ok)setData(j);else setMsg(j.error||"Unable to load recovery data.");}useEffect(()=>{void load()},[]);async function act(action:string,id:string,confirmText?:string){setBusy(id);setMsg("");const r=await fetch("/api/admin/recovery",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,id,confirm:confirmText})});const j=await r.json();if(!r.ok)setMsg(j.error||"Action failed.");else{setMsg("Action completed.");await load();}setBusy("");}function permanent(id:string){if(!confirm("Permanently delete this trashed update and its S.O.H-hosted files? This cannot be undone."))return;const c=prompt("Type PERMANENTLY DELETE to confirm.");if(c==="PERMANENTLY DELETE")void act("permanent-delete",id,c);}return <div><div className="mb-5 flex flex-wrap gap-2">{(["trash","history","audit"] as const).map(t=><button key={t} onClick={()=>setTab(t)} className={`rounded-xl px-4 py-2 text-sm font-black ${tab===t?"bg-green-700 text-white":"border bg-white text-gray-800"}`}>{t==="trash"?`Trash (${data.trash.length})`:t==="history"?`Version History (${data.history.length})`:`Audit Log (${data.audit.length})`}</button>)}</div>{msg&&<p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm font-bold text-amber-900">{msg}</p>}{tab==="trash"&&<div className="space-y-3">{data.trash.length?data.trash.map(s=>{const days=Math.max(0,30-Math.floor((Date.now()-+new Date(s.updated_at))/86400000));return <article key={s.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-black">{s.title}</h3><p className="mt-1 text-xs font-bold text-gray-500">Moved {new Date(s.updated_at).toLocaleString("en-NG")} · Recovery window: {days} day{days===1?"":"s"} remaining</p></div><div className="flex gap-2"><button disabled={busy===s.id} onClick={()=>void act("restore",s.id)} className="rounded-xl bg-green-700 px-4 py-2 text-sm font-black text-white">Restore</button><button disabled={busy===s.id} onClick={()=>permanent(s.id)} className="rounded-xl bg-red-600 px-4 py-2 text-sm font-black text-white">Delete Forever</button></div></div></article>}):<p className="rounded-2xl border bg-white p-6 text-sm text-gray-500">Trash is empty.</p>}</div>}{tab==="history"&&<div className="space-y-3">{data.history.length?data.history.map(h=><article key={h.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-black">{h.title}</h3><p className="mt-1 text-xs font-bold text-gray-500">{h.action.replaceAll("_"," ")} · {new Date(h.created_at).toLocaleString("en-NG")}</p><p className="mt-2 text-sm text-gray-600">Snapshot status: <b>{h.snapshot.status}</b> · {h.snapshot.category}</p></div><button disabled={busy===h.id} onClick={()=>{if(confirm("Restore this previous version? The current version will be saved first."))void act("restore-version",h.id)}} className="rounded-xl border border-green-300 bg-green-50 px-4 py-2 text-sm font-black text-green-900">Restore Version</button></div></article>):<p className="rounded-2xl border bg-white p-6 text-sm text-gray-500">No version history yet. New edits will automatically create snapshots.</p>}</div>}{tab==="audit"&&<div className="space-y-3">{data.audit.length?data.audit.map(a=><article key={a.id} className="rounded-2xl border bg-white p-4"><p className="font-black">{a.action}</p><p className="mt-1 text-sm font-bold text-gray-700">{a.title}</p><p className="mt-1 text-xs text-gray-500">{new Date(a.created_at).toLocaleString("en-NG")}{a.details?` · ${a.details}`:""}</p></article>):<p className="rounded-2xl border bg-white p-6 text-sm text-gray-500">No audit activity recorded yet.</p>}</div>}</div>}
