import Link from "next/link";
import { redirect } from "next/navigation";
import { isAdmin } from "../../../lib/admin-auth";
import RecoveryClient from "./RecoveryClient";
export default async function RecoveryPage(){if(!(await isAdmin()))redirect("/admin/updates");return <main className="min-h-screen bg-gray-50 text-gray-950"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-3 px-5 py-5"><div><p className="text-xs font-black uppercase tracking-[.18em] text-green-700">S.O.H CONSULTS · PRIVATE</p><h1 className="text-2xl font-black">Recovery & Accountability</h1><p className="mt-1 text-sm text-gray-500">Trash, previous versions and a private action trail.</p></div><div className="flex gap-2"><Link href="/admin" className="rounded-xl border px-4 py-2 text-sm font-black">Dashboard</Link><Link href="/admin/updates" className="rounded-xl bg-green-700 px-4 py-2 text-sm font-black text-white">Manage Updates</Link></div></div></header><section className="mx-auto max-w-6xl px-5 py-7"><div className="mb-5 rounded-2xl border border-green-200 bg-green-50 p-5"><h2 className="font-black text-green-950">Protected recovery layer</h2><p className="mt-1 text-sm font-medium text-green-900">Updates moved to Trash are hidden from normal management and public publishing. Restore brings them back to their previous status. Permanent deletion still requires the exact confirmation phrase. Version snapshots are captured before edits and restores.</p></div><RecoveryClient/></section></main>}
