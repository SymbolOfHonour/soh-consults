"use client";import{useEffect,useState}from"react";type C={enabled:boolean;title:string;message:string;link:string;label:string;tone:"green"|"amber"|"red"};const init:C={enabled:false,title:"Important Update",message:"",link:"/updates",label:"Read update",tone:"green"};export default function Client(){const[c,setC]=useState(init),[busy,setBusy]=useState(false),[msg,setMsg]=useState("");useEffect(()=>{fetch("/api/admin/homepage").then(r=>r.json()).then(setC)},[]);async function save(){setBusy(true);const r=await fetch("/api/admin/homepage",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(c)}),j=await r.json();setMsg(r.ok?"Homepage announcement saved.":j.error||"Unable to save.");if(r.ok)setC(j);setBusy(false)}return <div className="rounded-3xl border bg-white p-6 shadow-sm"><div className="flex items-center justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-wider text-green-700">Public homepage</p><h2 className="text-2xl font-black">Announcement Banner</h2></div><label className="flex items-center gap-2 font-black"><input type="checkbox" checked={c.enabled} onChange={e=>setC({...c,enabled:e.target.checked})}/> Live</label></div><div className="mt-6 grid gap-4 md:grid-cols-2"><label className="text-sm font-black">Heading<input value={c.title} onChange={e=>setC({...c,title:e.target.value})} className="mt-1 w-full rounded-xl border p-3"/></label><label className="text-sm font-black">Tone<select value={c.tone} onChange={e=>setC({...c,tone:e.target.value as C["tone"]})} className="mt-1 w-full rounded-xl border p-3"><option value="green">Green</option><option value="amber">Amber</option><option value="red">Red</option></select></label><label className="md:col-span-2 text-sm font-black">Message<textarea value={c.message} onChange={e=>setC({...c,message:e.target.value})} rows={3} className="mt-1 w-full rounded-xl border p-3"/></label><label className="text-sm font-black">Button link<input value={c.link} onChange={e=>setC({...c,link:e.target.value})} className="mt-1 w-full rounded-xl border p-3"/></label><label className="text-sm font-black">Button label<input value={c.label} onChange={e=>setC({...c,label:e.target.value})} className="mt-1 w-full rounded-xl border p-3"/></label></div>{msg&&<p className="mt-4 rounded-xl bg-gray-50 p-3 text-sm font-bold">{msg}</p>}<button disabled={busy} onClick={()=>void save()} className="mt-5 rounded-xl bg-green-700 px-5 py-3 font-black text-white disabled:opacity-50">{busy?"Saving...":"Save Homepage Control"}</button></div>}
