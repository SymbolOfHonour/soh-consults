"use client";
import {useMemo,useState} from "react";
type Item={kind:"image"|"document";url:string;name:string;article:string};
export default function MediaIntelligenceClient({items}:{items:Item[]}){const [query,setQuery]=useState(""),[kind,setKind]=useState("all"),[copied,setCopied]=useState("");const filtered=useMemo(()=>items.filter(item=>(kind==="all"||item.kind===kind)&&(!query.trim()||`${item.name} ${item.article} ${item.url}`.toLowerCase().includes(query.toLowerCase()))),[items,query,kind]);async function copy(url:string){await navigator.clipboard.writeText(url);setCopied(url);setTimeout(()=>setCopied(""),1600)}return <section className="mb-6 rounded-2xl border border-green-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-wider text-green-700">Phase 3.3 · Media intelligence</p><h2 className="text-xl font-black">Searchable reusable media ({filtered.length}/{items.length})</h2></div><div className="flex flex-wrap gap-2"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search file or article..." className="rounded-xl border px-3 py-2 text-sm"/><select value={kind} onChange={e=>setKind(e.target.value)} className="rounded-xl border px-3 py-2 text-sm font-bold"><option value="all">All media</option><option value="image">Images</option><option value="document">Documents</option></select></div></div>{filtered.length===0?<p className="mt-4 rounded-xl bg-gray-50 p-4 text-sm">No media matches this search.</p>:<div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(item=><article key={item.url} className="min-w-0 rounded-xl border p-3">{item.kind==="image"?<img src={item.url} alt={item.name} loading="lazy" className="h-40 w-full rounded-lg object-contain"/>:<div className="flex h-40 items-center justify-center rounded-lg bg-gray-50 text-4xl">📄</div>}<p className="mt-2 truncate text-sm font-black" title={item.name}>{item.name}</p><p className="truncate text-xs text-gray-500" title={item.article}>{item.article}</p><div className="mt-3 flex gap-2"><button onClick={()=>void copy(item.url)} className="rounded-lg bg-green-700 px-3 py-2 text-xs font-black text-white">{copied===item.url?"Copied ✓":"Copy URL"}</button><a href={item.url} target="_blank" rel="noopener noreferrer" className="rounded-lg border px-3 py-2 text-xs font-black">Open ↗</a></div></article>)}</div>}</section>}
