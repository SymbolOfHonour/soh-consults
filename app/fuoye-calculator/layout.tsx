import type { Metadata } from "next";
export const metadata:Metadata={title:"FUOYE Screening Score Calculator | S.O.H CONSULTS",description:"Calculate your FUOYE screening score from JAMB score, O'Level grades and sitting bonus, with PDF and image report downloads.",alternates:{canonical:"/fuoye-calculator"}};
export default function Layout({children}:{children:React.ReactNode}){return children}