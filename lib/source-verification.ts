type Verification={ok:boolean;reason:string};
function isPrivateHost(host:string){const h=host.toLowerCase();if(h==="localhost"||h.endsWith(".local"))return true;if(/^127\./.test(h)||/^10\./.test(h)||/^192\.168\./.test(h)||/^169\.254\./.test(h))return true;const m=h.match(/^172\.(\d{1,3})\./);if(m){const n=Number(m[1]);if(n>=16&&n<=31)return true;}if(h==="::1"||h.startsWith("fc")||h.startsWith("fd")||h.startsWith("fe80:"))return true;return false;}
function safeExternalUrl(value:string|null|undefined){if(!value)return null;try{const u=new URL(value);if(!["http:","https:"].includes(u.protocol)||isPrivateHost(u.hostname))return null;u.username="";u.password="";return u;}catch{return null}}
export async function verifyExternalSource(value:string|null|undefined):Promise<Verification>{const url=safeExternalUrl(value);if(!url)return{ok:false,reason:"Official source URL is invalid or points to a private/local address."};try{let response=await fetch(url,{method:"HEAD",redirect:"follow",cache:"no-store",signal:AbortSignal.timeout(8000),headers:{"User-Agent":"S.O.H CONSULTS publication verifier (+https://soh-consults.vercel.app)"}});if(response.status===405||response.status===501)response=await fetch(url,{method:"GET",redirect:"follow",cache:"no-store",signal:AbortSignal.timeout(8000),headers:{"User-Agent":"S.O.H CONSULTS publication verifier (+https://soh-consults.vercel.app)",Range:"bytes=0-0"}});if(!response.ok)return{ok:false,reason:`Official source could not be reached successfully (HTTP ${response.status}).`};const finalUrl=safeExternalUrl(response.url||url.toString());if(!finalUrl)return{ok:false,reason:"Official source redirected to an unsafe address."};return{ok:true,reason:"Official source is reachable."};}catch(error){return{ok:false,reason:error instanceof Error?`Official source could not be verified: ${error.message}`:"Official source could not be verified."};}}
export function deadlineEvidencePresent(deadline:string|null|undefined,deadlineIso:string|null|undefined,officialSourceUrl:string|null|undefined){if(!deadline&&!deadlineIso)return true;if(!officialSourceUrl)return false;if(!deadlineIso||Number.isNaN(Date.parse(deadlineIso)))return false;return true;}
