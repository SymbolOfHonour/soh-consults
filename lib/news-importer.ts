import { upsertStories, type QueuedStory } from "./news-queue";
import { newsSources, type NewsSource } from "./news-sources";

const useful = /admission|post[- ]?utme|direct entry|screening|jamb|waec|wassce|neco|nabteb|scholarship|application|admission list|cut[- ]?off|nysc|resumption|clearance|acceptance fee|registration|timetable|mobilisation|mobilization|matriculation|convocation|deadline|result|bursary|fellowship|exam|o.level/i;
const exclude = /software download|photo gallery|courtesy visit|stakeholders? meeting|advertorial|procurement|tender|staff recruitment|birthday|condolence|anniversary message/i;
const FRESH_HOURS = 168;
const USER_AGENT = "S.O.H CONSULTS education update monitor (+https://sohconsults.com.ng)";
function decode(v: string) { return v.replace(/<[^>]+>/g, " ").replace(/&amp;/g, "&").replace(/&#8211;|&ndash;/g, "-").replace(/&#8217;|&rsquo;/g, "'").replace(/&quot;|&#8220;|&#8221;/g, '"').replace(/&#(\d+);/g, (_, c) => String.fromCharCode(Number(c))).replace(/\s+/g, " ").trim(); }
function category(t: string) { if (/scholarship|bursary|fellowship/i.test(t)) return "Scholarship"; if (/jamb|utme|direct entry/i.test(t)) return "JAMB"; if (/waec|wassce|neco|nabteb|o.level/i.test(t)) return "O'Level"; if (/nysc|mobilisation|mobilization/i.test(t)) return "NYSC"; if (/admission list/i.test(t)) return "Admission List"; return "Admission"; }
function institution(t: string, s: NewsSource) { return s.official ? s.name : t.match(/^([A-Z][A-Z0-9-]{2,12})(?:\s|:)/)?.[1] || "To be confirmed"; }
function links(html: string, s: NewsSource) { const found = new Map<string, { title: string; url: string }>(); const host = new URL(s.baseUrl).hostname.replace(/^www\./, ""); for (const match of html.matchAll(/<a\b[^>]*href=["']([^"'#]+)["'][^>]*>([\s\S]*?)<\/a>/gi)) { const title = decode(match[2]); if (title.length < 20 || title.length > 180 || !useful.test(title) || exclude.test(title)) continue; try { const url = new URL(match[1], s.baseUrl); if (url.protocol !== "https:" || url.hostname.replace(/^www\./, "") !== host) continue; url.hash = ""; found.set(url.toString(), { title, url: url.toString() }); } catch { /* Invalid source link. */ } } return [...found.values()].slice(0, s.official ? 8 : 20); }
/** Article metadata, not crawl time, determines freshness. */
export function articlePublicationDate(html: string): string | null { for (const tag of html.match(/<meta\b[^>]*>/gi) || []) { const key = tag.match(/(?:property|name)\s*=\s*["']([^"']+)["']/i)?.[1]?.toLowerCase(); if (!key || !["article:published_time", "datepublished", "pubdate", "publish_date", "dc.date.issued", "citation_publication_date"].includes(key)) continue; const value = tag.match(/content\s*=\s*["']([^"']+)["']/i)?.[1]; if (value && !Number.isNaN(Date.parse(value))) return new Date(value).toISOString(); } for (const match of html.matchAll(/"datePublished"\s*:\s*"([^"\n]+)"/gi)) if (!Number.isNaN(Date.parse(match[1]))) return new Date(match[1]).toISOString(); return null; }
export function isFreshPublication(date: string | null, now = Date.now()) { if (!date) return false; const time = Date.parse(date); return Number.isFinite(time) && time <= now + 300000 && time >= now - FRESH_HOURS * 3600000; }
export type DiscoverySuggestion = { source_name: string; source_url: string; source_published_at: string | null; title: string; institution: string; category: string; official: boolean };
async function retrieve(url: string, timeout = 9000) { const response = await fetch(url, { headers: { "User-Agent": USER_AGENT }, cache: "no-store", signal: AbortSignal.timeout(timeout) }); if (!response.ok) throw new Error(`HTTP ${response.status}`); return response.text(); }
export async function discoverLatestStories() { const suggestions: DiscoverySuggestion[] = []; const failures: string[] = []; let skippedOld = 0, skippedUndated = 0; let next = 0;
  // Bound source concurrency to avoid overwhelming third-party websites or serverless limits.
  async function worker() { while (next < newsSources.length) { const source = newsSources[next++]; try { const found = links(await retrieve(source.url, 12000), source); if (!found.length) { failures.push(`${source.name}: no qualifying links; review source layout.`); continue; } let cursor = 0; async function articleWorker() { while (cursor < found.length) { const item = found[cursor++]; try { const published = articlePublicationDate(await retrieve(item.url)); if (!published) skippedUndated++; else if (!isFreshPublication(published)) { skippedOld++; continue; } suggestions.push({ source_name: source.name, source_url: item.url, source_published_at: published, title: item.title, institution: institution(item.title, source), category: category(item.title), official: !!source.official }); } catch (error) { failures.push(`${source.name}: ${item.url}: ${error instanceof Error ? error.message : "Article fetch failed"}`); } } } await Promise.all(Array.from({ length: Math.min(3, found.length) }, () => articleWorker())); } catch (error) { failures.push(`${source.name}: ${error instanceof Error ? error.message : "Source fetch failed"}`); } } }
  await Promise.all(Array.from({ length: Math.min(3, newsSources.length) }, () => worker())); suggestions.sort((a, b) => (b.source_published_at || "").localeCompare(a.source_published_at || "")); return { suggestions, failures, skippedOld, skippedUndated, sourcesChecked: newsSources.length }; }
export async function importLatestStories() { const { suggestions, failures, skippedOld, skippedUndated, sourcesChecked } = await discoverLatestStories(); const drafts: Partial<QueuedStory>[] = suggestions.map(item => ({ source_name: item.source_name, source_url: item.source_url, source_published_at: item.source_published_at, title: item.title, institution: item.institution, category: item.category, summary: `${item.title}. ${item.official ? "Official-source" : "Discovery-source"} draft. Verify the original article and prepare an original S.O.H CONSULTS summary before approval.`, details: item.source_published_at ? "Verify dates, requirements and application links before publishing." : "Publication date unverified. Confirm all details before publishing.", official_source_name: item.official ? item.source_name : "", official_source_url: item.official ? item.source_url : "", status: "draft" })); const saved = await upsertStories(drafts); return { discovered: drafts.length, added: saved.length, skippedOld, skippedUndated, sourcesChecked, failures }; }
